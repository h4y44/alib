/*
 *heap sort
 */
#include <stdio.h>
#include <stdlib.h>
#include "btree.h"
#include <limits.h>
 
static inline max_in_arr(int arr[], size_t len) {
	for (int )
}
 
int heapify(btree_t *root, int arr[], size_t len, size_t start) {
 	if (start == size -1) {
 		return 0;
	}
	int max = arr[start];
 	for (int i = start ; i< len; ++i) {
 		
	}
}


